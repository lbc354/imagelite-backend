spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/imagelite_db
    username: postgres
    password: postgresql_password
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false